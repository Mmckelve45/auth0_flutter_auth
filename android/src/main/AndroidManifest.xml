<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.auth0.flutter_auth">

    <!-- CallbackActivity is declared here without an intent-filter.
         The consuming app MUST re-declare it in their own AndroidManifest.xml
         with an intent-filter matching their callback URL scheme.
         See the example app's AndroidManifest.xml for reference. -->

</manifest>
